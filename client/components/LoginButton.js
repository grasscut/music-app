import React from 'react';

const LoginButton = () => {
    return (
        <a className="loginButton" href="/login">
            Login with Spotify
        </a>
    );
};

export default LoginButton;